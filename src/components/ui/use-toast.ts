
// Re-export the toast hooks from our actual implementation
import { useToast, toast, type ToasterToast, type ToastProps } from "@/hooks/use-toast"

export { useToast, toast, type ToasterToast, type ToastProps }
